# Server Configuration
PORT=4444

# Authentication Token (REQUIRED for production)
# Generate a secure random token: openssl rand -hex 32
# This token must be included in the WebSocket URL: ws://server:4444?token=YOUR_TOKEN
AUTH_TOKEN=your-secure-token-here

# Allowed Origins (comma-separated)
# Only connections from these origins will be accepted
# Default: https://gdsjam.com,http://localhost:5173,http://localhost:4173
ALLOWED_ORIGINS=https://gdsjam.com,http://localhost:5173,http://localhost:5174,http://localhost:4173

# WebSocket Rate Limiting (optional - defaults shown)
# RATE_LIMIT_WINDOW=60000  # 1 minute in milliseconds
# RATE_LIMIT_MAX_CONNECTIONS=10  # Max WebSocket connections per IP per window

# File Upload Rate Limiting (optional - defaults shown)
# UPLOAD_RATE_LIMIT_WINDOW=3600000  # 1 hour in milliseconds
# UPLOAD_RATE_LIMIT_MAX=100  # Max uploads per IP per hour
# WEEKLY_SIZE_LIMIT_MB=1000  # Max total upload size per IP per week (1GB default)

# Python Execution Configuration (optional - defaults shown)
# PYTHON_VENV_PATH=/opt/gdsjam/venv  # Path to Python virtual environment
# PYTHON_TIMEOUT=30  # Execution timeout in seconds
# PYTHON_RATE_LIMIT_WINDOW=60000  # 1 minute in milliseconds
# PYTHON_RATE_LIMIT_MAX=10  # Max executions per IP per window
# MAX_GDS_SIZE_MB=100  # Max size of generated GDS files
